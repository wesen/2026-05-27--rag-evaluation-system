/**
 * css-entry.ts
 *
 * Entry point for a Vite client build that produces a single CSS file
 * containing all design system styles. The JS is discarded; we only
 * need the CSS output.
 */

import '../../src/index.css';
